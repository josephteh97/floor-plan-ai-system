name: floorplan-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy
  - opencv
  - pillow
  - pandas
  - nodejs=18
  - pip:
      - fastapi
      - uvicorn[standard]
      - python-multipart
      - anthropic
      - pydantic
      - python-dotenv
      - aiofiles